#include <string>
#include <fstream>
using namespace std;

int main() {
    ifstream in("Scopy.cpp"); // Open for reading
    ofstream out("Scoppy2.cpp"); // bOpen for writing
    string s;
    while(getline(in, s)) // Discards newline char
}

